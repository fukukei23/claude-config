# 軽量開発品質管理ルール

## Tier 1 タスクの自動判定

以下のキーワードを含むタスクは **Tier 1（高リスク）** として扱い、実装前に簡易仕様確認を必須とする。

### Tier 1 判定キーワード

#### 認証・認可関連
- 認証、認可、ログイン、ログアウト
- パスワード、トークン、セッション
- JWT、OAuth、SAML
- 権限、ロール、アクセス制御
- 多要素認証、MFA、2FA

#### 決済・金融関連
- 決済、支払い、課金、請求
- クレジットカード、銀行口座
- 返金、チャージバック
- 金額計算、税金計算

#### データ管理関連
- データ移行、マイグレーション
- スキーマ変更、テーブル変更
- データ削除、一括削除
- バックアップ、リストア

#### 公開API関連
- 公開API、エンドポイント
- APIキー、シークレット
- レート制限、スロットリング
- Webhook

#### セキュリティ関連
- 暗号化、復号化
- ハッシュ化、ソルト
- CSRF、XSS、SQLインジェクション
- セキュリティヘッダー
- CORS設定

#### インフラ・デプロイ関連
- 本番環境、プロダクション
- デプロイ、リリース
- ロールバック、切り戻し
- DNS変更、ドメイン設定

### Tier 1 タスクの処理フロー

Tier 1 タスクを検出した場合、以下のステップを実行する:

1. **判定通知**
   ```
   このタスクは Tier 1（[該当カテゴリ]）です。
   簡易仕様を確認させてください。
   ```

2. **簡易仕様確認（3項目）**
   ```
   1. 何を作るか: [機能の概要を1-2文で]
   2. 主要な制約:
      - [制約1]
      - [制約2]
      - [制約3]
   3. 影響範囲: 
      - 新規ファイル作成 or 既存ファイル修正
      - 変更対象ファイル: [ファイル名]
   ```

3. **ユーザー確認**
   ```
   この内容で実装を開始してよろしいですか？
   ```

4. **実装開始**
   - ユーザーが承認後に実装を開始
   - 重要な判断は自動で `docs/decisions/` に記録

### Tier 2 タスク（通常）

Tier 1 キーワードに該当しないタスクは即座に実装を開始してよい。

#### 例
- UI コンポーネントの作成
- 既存機能の軽微な修正
- ログ出力の追加
- テストコードの追加
- ドキュメント更新

## 自動エージェント起動ルール

### Tier 1 Validator Agent
Tier 1 キーワードを検出した場合、自動的に起動し、仕様確認を実施する。

### Code Reviewer Agent
以下のトリガーで起動:
- ユーザーが「レビューして」と指示
- ユーザーが「今週のコード」「今日のコミット」を確認要求
- 実装完了後、ユーザーがレビューを希望

### Decision Recorder Agent
以下のトリガーで起動:
- ユーザーが「〜を採用する」「〜に決めた」と判断を表明
- Tier 1 実装中に重要な設計判断が発生
- ユーザーが明示的に記録を依頼

## 開発フロー

### 通常フロー（Tier 2）
```
ユーザー指示 → 直接実装
```

### Tier 1 フロー
```
ユーザー指示 
  → Tier 1 Validator 判定
  → 簡易仕様確認（3項目）
  → ユーザー承認
  → 実装開始
  → 重要判断を Decision Recorder で記録
  → 完了後 Code Reviewer でレビュー（オプション）
```

### 定期レビューフロー
```
ユーザー「今週のコードをレビューして」
  → Code Reviewer Agent 起動
  → 並列で複数ファイルを分析
  → 結果をサマリー表示
```

## ファイル構成規約

### 決定記録
- 場所: `docs/decisions/`
- 命名: `001-use-postgresql.md`（連番 + 内容）

### 仕様書（Tier 1 のみ）
- 場所: `docs/specs/`
- 命名: `tier1-[feature-name].md`

### ワークフロー定義
- 場所: `.claude/workflows/`
- 参照: `development-flow.md`

## 注意事項

- Tier 1 判定は保守的に実施（疑わしい場合は Tier 1 扱い）
- 仕様確認は最小限（3項目のみ）で実施
- ユーザーの承認なしに Tier 1 実装を開始しない
- 決定記録は重要な判断のみ（些細な選択は記録不要）
