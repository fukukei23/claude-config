# Zenn人気タグリスト

## 検索流入が多いタグ（優先度高）

### 言語・フレームワーク
`python` / `typescript` / `javascript` / `go` / `rust` / `react` / `nextjs` / `flask` / `fastapi` / `django` / `nodejs`

### AI・LLM
`ai` / `llm` / `openai` / `claude` / `chatgpt` / `deeplearning` / `machinelearning`

### インフラ・ツール
`docker` / `linux` / `git` / `github` / `aws` / `gcp` / `terraform` / `ci` / `webpack` / `vite`

### セキュリティ・品質
`security` / `testing` / `tdd` / `cicd`

### その他
`automation` / `api` / `webdev` / `beginners` / `personaldevelopment` / `cli` / `wsl` / `scraping` / `obsidian` / `playwright` / `selenium` / `stripe`

## 固有名詞タグ（そのまま維持）

`openclaw` / `mcp` / `obsidian` / `playwright` / `selenium` / `stripe` / `flask`

## タグ置換マップ

ニッチタグを人気タグに置換する際の対応表:

| ニッチタグ | → 人気タグ | 理由 |
|-----------|-----------|------|
| `claudecode` | `claude` | 検索ボリューム大きい |
| `vps` | `linux` / `docker` | 一般タグの方が流入多い |
| `glm` | `llm` | 一般カテゴリの方が検索される |
| `cost` | `automation` / `llm` | コスト単体では検索されにくい |
| `routing` | `api` / `automation` | 同上 |
| `cron` | `automation` / `linux` | 同上 |
| `proxy` | `api` / `automation` | 同上 |
| `career` | `personaldevelopment` | Zenn人気タグ |
| `config` | `beginners` / `git` | 記事内容に応じて |
| `ratelimit` | `api` / `automation` | ニッチすぎる |
| `threatmodeling` | `security` / `beginners` | より広いカテゴリ |
| `hmac` | `security` / `api` | 同上 |
| `webhook` | `api` / `security` | 同上 |
| `zenn` | `automation` / `github` | Zennタグは流入少ない |
| `education` / `learning` | `beginners` | Zenn人気タグ |
| `hooks` | `automation` / `git` | Claude Code hooks は automation で表現 |
| `taskmanagement` | `automation` / `personaldevelopment` | 一般タグに |

## topics数のルール

- **3〜5個**（Zenn最大5個）
- **人気タグ2個** + **固有名詞1個** + **内容タグ1〜2個** が基本構成
- 初心者向け記事には必ず `beginners` を含める
