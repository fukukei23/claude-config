今日やることを順番に実行して。完了したら次のタスクに進むこと。不明点があれば都度確認して。

タスク1: WSL2起動時のtmux自動起動
systemd=true が /etc/wsl.conf に設定済み。systemdユーザーサービスとして設定すること。
要件

セッション名: krokod
起動コマンド: BROWSER=none claude --channels plugin:discord@claude-plugins-official
ユーザー: yn441611
セッションが既に存在する場合は何もしない（冪等性）
WSL2再起動後に自動でセッションが存在することを確認する

完了条件

systemctl --user status <サービス名> が active になる
tmux ls で krokod セッションが確認できる


タスク2: SSH鍵設定（クロコド→フクロウ・よつば）
クロコドからフクロウ・よつばにパスワードなしでSSH接続できるようにする。
対象ホスト

フクロウ: op@162.43.17.111（SSH設定名: openclaw-vps）
よつば: user@192.168.1.7（SSH設定名: claw-node）、またはTailscale経由 user@100.78.104.58

要件

既存の ~/.ssh/id_ed25519 があれば流用。なければ新規作成（ed25519）
~/.ssh/config に設定済みか確認。未設定なら追加
ssh-copy-id またはAPIで公開鍵を各ホストに登録
パスワードなし接続を ssh openclaw-vps および ssh claw-node で確認


タスク3: よつば実機確認（SSH鍵設定後に実施）
SSH接続後、以下の3点を確認して結果を報告すること。
3-1. workspaceパス確認
bashdocker exec <コンテナ名> cat /home/node/.openclaw/workspace/yotsuba/AGENTS.md | head -5
# エラーの場合: sessions.jsonリセット
# docker compose down && rm ~/.openclaw/sessions.json && docker compose up -d
3-2. MiniMax baseUrl確認
bashgrep -A3 "minimax" ~/.openclaw/openclaw.json

https://api.minimax.io/v1 か https://api.minimax.io/anthropic か記録すること
フクロウは /anthropic が正。よつばが /v1 のままなら意図的かどうか判断が必要

3-3. パッケージ確認
bashdocker exec <コンテナ名> which ffmpeg yt-dlp pandoc

存在しないパッケージがあれば報告すること（Dockerfileへの追記が必要）


完了後
各タスクの結果をまとめて報告すること。特に以下を明記：

タスク2: 生成または使用した公開鍵の内容（cat ~/.ssh/id_ed25519.pub）
タスク3-2: よつばのMiniMax baseUrlの実際の値
タスク3-3: 存在しなかったパッケージの一覧