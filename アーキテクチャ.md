# Claude Code Configuration Architecture

2環境（WSL2 CLI / Windows Desktop）の構成と、SSOTへの集約状況。

## 構成図

```
┌─────────────────────────────────────────────────┐
│                  SSOT (共通知識ベース)              │
│         /home/yn4416/projects/obsidian-ssot/     │
│         GitHub: fukukei23/obsidian-ssot          │
│                                                   │
│  00_SYSTEM/共通ルール/                            │
│  ├── ルール.md         ← 禁止操作・Tier1・確認必須   │
│  ├── LLMルーティング.md ← LLM振り分け・バッジ表示    │
│  ├── セッションログ書式.md ← ログフォーマット       │
│  └── 場所.md           ← パス情報                  │
│                                                   │
│  01_DECISIONS/          ← 意思決定記録              │
│  10_DAILY/              ← セッションログ             │
│  projects/              ← プロジェクト別情報         │
│  99_ARCHIVE/            ← 旧ファイル                │
└────────────────────┬────────────────────────────┘
                     │ 参照
         ┌───────────┴───────────┐
         ▼                       ▼
┌─────────────────┐    ┌──────────────────┐
│  WSL2 CLI       │    │  Windows Desktop   │
│ ~/.claude/      │    │ C:\Users\USER\     │
│                 │    │   .claude\         │
│ CLAUDE.md       │    │ CLAUDE.md          │
│ scripts/        │    │ agents/            │
│  ├── glm-mcp    │    │ core/              │
│  ├── minimax-mcp│    │ docs/              │
│  ├── glm-rate-  │    │ lib/               │
│  │   proxy/     │    │ rules/workspace.md │
│  ├── save-log   │    │ scheduled-tasks/   │
│  └── load-log   │    │ tools/             │
│ skills/         │    │ workflows/         │
│ settings.json   │    │ settings.json      │
│ settings.local  │    │ settings.local.json│
│ .env            │    │ .claude-wrapper    │
│                 │    │ .env               │
│ [glm-rate-proxy]│    │                    │
│ localhost:8787  │    │ [claude-mem]       │
│ ↓               │    │ C:\~\.claude-mem\  │
│ ZAI / MiniMax   │    │ Sonnet OAuth       │
└─────────────────┘    └──────────────────────┘
```

## CLI版: glm-rate-proxy（ローカルプロキシ）

CLI版のリクエストは `glm-rate-proxy` 経由で外部APIに到達する。

```
Claude Code CLI
    │
    ▼ http://127.0.0.1:8787
┌─────────────────────────────────────────────┐
│            glm-rate-proxy                    │
│  ┌─────────────┐  ┌──────────────────────┐  │
│  │ 使用率監視   │  │ ピークブロック(15-19時)│  │
│  │ <80%: 5.1   │  │ → MiniMax強制        │  │
│  │ 80-95%: 4.7 │  └──────────────────────┘  │
│  │ >95%: Flash │  ┌──────────────────────┐  │
│  └─────────────┘  │ エラーフォールバック   │  │
│                    │ 429/500/timeout      │  │
│                    │ → MiniMax自動切替    │  │
│                    └──────────────────────┘  │
│                         │                    │
│           ┌─────────────┴─────────────┐      │
│           ▼                           ▼      │
│     Z.AI API                    MiniMax API   │
│     (GLM-5.1)                  (M2.7)        │
└─────────────────────────────────────────────┘

※ プロキシ未起動時: sync-secrets-to-settings.sh が
   ZAI直結URLに自動フォールバック（安全なデフォルト）
```

## claude-mem（永続メモリプラグイン）

両環境にインストール済み。操作ログを自動記録し、次回セッション冒頭に注入。

| 項目 | WSL2 CLI | Windows Desktop |
|------|----------|-----------------|
| バージョン | v12.1.0 | v12.1.0 |
| 要約API | Z.ai経由（GLMトークン消費） | Claude OAuth（サブスク枠内） |
| データ場所 | `~/.claude-mem/` | `C:\Users\USER\.claude-mem\` |
| 推奨モデル | デフォルト（Sonnet） | Haiku（コスト最適化推奨） |
| Web Viewer | `http://localhost:37777` | `http://localhost:37777` |

3層メモリ設計の第1層（自動操作ログ）を担当。詳細は `claude-mem/README.md` 参照。

## ファイル分類

### 共通（SSOTに集約済み）
以下はCLAUDE.md/rulesから削除済み。SSOTが単一ソース。

| 内容 | SSOTの場所 |
|---|---|
| 禁止操作 | `00_SYSTEM/shared-rules/ルール.md` |
| Tier1キーワード | `00_SYSTEM/shared-rules/ルール.md` |
| 確認必須操作 | `00_SYSTEM/shared-rules/ルール.md` |
| LLMルーティング | `00_SYSTEM/shared-rules/LLMルーティング.md` |
| バッジ表示ルール | `00_SYSTEM/shared-rules/LLMルーティング.md` |
| セッションログフォーマット | `00_SYSTEM/shared-rules/セッションログ書式.md` |
| 開発フロー | `00_SYSTEM/shared-rules/ルール.md` |

### 環境固有（各.claude/に残す）

#### WSL2 CLI固有
| ファイル | 用途 |
|---|---|
| `scripts/glm-rate-proxy/` | ローカルプロキシ本体（Python/aiohttp） |
| `scripts/start-glm-proxy.sh` | SessionStart hook でプロキシ自動起動 |
| `scripts/sync-secrets-to-settings.sh` | プロキシ生存チェック→URL切替 |
| `scripts/glm-mcp-server.py` | GLM MCPサーバー（18ツール） |
| `scripts/minimax-mcp-server.py` | MiniMax MCPサーバー（17ツール） |
| `scripts/save-session-log.sh` | セッション終了時ログ保存 |
| `scripts/load-obsidian-log.sh` | セッション開始時ログ読込 |
| `~/.secrets.env` | APIキー一元管理（秘匿）|
| `settings.json` | メイン設定（SessionStartで動的注入） |
| `settings.local.json` | 権限設定 |
| `scripts/glm-rate-proxy/config/config.json` | プロキシ設定（ピーク時間・閾値） |

#### Windows Desktop固有
| ファイル | 用途 |
|---|---|
| `agents/コードレビュー.md` | コードレビューエージェント |
| `agents/判断記録.md` | 意思決定記録エージェント |
| `agents/tier1バリデーター.md` | Tier1検証エージェント |
| `core/` | Windows自動化スクリプト群 |
| `lib/` | JS wrapper/LLM pool管理 |
| `docs/コンピュータ使用.md` | Windows自動操作ガイド |
| `docs/フォールバック.md` | フォールバック手順 |
| `docs/トラブルシューティング.md` | 2環境分離ドキュメント |
| `docs/decisions/` | 意思決定記録 |
| `rules/workspace.md` | ワークスペース管理 |
| `scheduled-tasks/daily-handover/` | 日次引き継ぎ自動化 |
| `scheduled-tasks/glm-cost-tracker/` | GLMコスト追跡 |
| `tools/` | ユーティリティツール |
| `.claude-wrapper.json` | LLMプール設定 |
| `create-obsidian-vault.md` | Obsidian構築ガイド |

### 削除済み（重複）
2026-04-04に重複排除で削除:

| ファイル | 理由 |
|---|---|
| WSL `rules.md` | SSOT rules.mdに統合 |
| WSL `rules/automation.md` | SSOT rules.md + CHARTERに統合 |
| WSL `rules/quality.md` | SSOT rules.mdに統合 |
| Windows `rules.md` | SSOT rules.mdに統合 |
| Windows `rules/llm-routing.md` | SSOT llm-routing.mdに統合 |
| Windows `rules/dev-workflow.md` | SSOT rules.mdに統合 |
| Windows `workflows/development-flow.md` | SSOT rules.mdに統合 |

## 削除・移動禁止ディレクトリ

`core/`, `lib/`, `tools/`, `docs/`, `agents/`, `scheduled-tasks/` は削除・移動禁止。
クリーンアップは提案のみ。自動実行しない。

## シークレット注入フロー（CLI版）

```
~/.secrets.env  ←  単一ソース（全APIキー）
    │
    ▼ SessionStart: scripts/config/sync-secrets-to-settings.sh
settings.json
  ├── env.ANTHROPIC_AUTH_TOKEN     ← Claude Code本体の認証
  ├── env.ANTHROPIC_BASE_URL       ← プロキシ生死で動的切替（重要）
  ├── env.BRAVE_API_KEY
  ├── mcpServers.glm.env.GLM_API_KEY
  ├── mcpServers.minimax.env.MINIMAX_API_KEY
  ├── mcpServers.brave-search.env.BRAVE_API_KEY
  └── mcpServers.github.env.GITHUB_PERSONAL_ACCESS_TOKEN
```

- `settings.json` は `.gitignore` で除外済み → GitHubに漏洩しない
- `env.ANTHROPIC_BASE_URL` はプロキシ起動中なら `http://127.0.0.1:8787`、
  停止中なら `~/.secrets.env` の ZAI直結URL に自動フォールバック
- MCPサーバーは別プロセス起動のため、必ず `mcpServers.*.env` への個別注入が必要

## 注意事項

- 2環境は完全に独立（認証方法も異なる）
- Desktop: Anthropic OAuth（Sonnet）
- CLI: settings.json env動的注入（GLM-5.1）
- `settings.json` の値はSessionStart毎に `~/.secrets.env` から再注入される
- `settings.json` および `~/.secrets.env` は `.gitignore` で除外 → GitHubにpushしない
- 共通ルールの変更はSSOTで行うこと
